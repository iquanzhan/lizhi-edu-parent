server:
  port: 10000
spring:
  application:
    name: lizhi-edu-admin-server
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: nacos-common.yml
        extension-configs:
          - data-id: nacos-common.yml
            refresh: true